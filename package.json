{
  "name": "orly",
  "version": "1.0.0",
  "description": "web server for evenathing",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p dist/js && node_modules/browserify/bin/cmd.js assets/js/index.js -o dist/js/index.js",
    "start": "npm run build && node app.js"
  },
  "repository": {
    "type": "git",
    "url": "github.com/unenglishable/orly.git"
  },
  "author": "unenglishable",
  "license": "ISC",
  "dependencies": {
    "browserify": "^12.0.2",
    "dotenv": "^1.2.0",
    "express": "^4.13.3",
    "mithril": "^0.2.0"
  }
}
